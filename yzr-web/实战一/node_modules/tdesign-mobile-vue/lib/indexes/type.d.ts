export interface TdIndexesProps {
    height?: number;
    list: ListItem[];
    sticky?: Boolean;
    onSelect?: (indexes: {
        groupIndex: string;
        childrenIndex: number;
    }) => void;
}
export interface ListItem {
    title: string;
    index: string;
    children: {
        title: string;
        [key: string]: any;
    }[];
}
