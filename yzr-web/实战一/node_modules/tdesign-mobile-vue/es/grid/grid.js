/**
 * tdesign v0.12.1
 * (c) 2022 TDesign Group
 * @license MIT
 */

import { defineComponent, toRefs, provide, openBlock, createElementBlock, normalizeClass, renderSlot } from 'vue';
import config from '../config.js';
import gridProps from './props.js';

var prefix = config.prefix;
var name = "".concat(prefix, "-grid");
var script = defineComponent({
  name: name,
  props: gridProps,
  setup: function setup(props) {
    var _toRefs = toRefs(props),
      column = _toRefs.column,
      gutter = _toRefs.gutter,
      border = _toRefs.border,
      align = _toRefs.align;
    provide("grid", {
      column: column,
      gutter: gutter,
      border: border,
      align: align
    });
    return {
      name: name
    };
  }
});

function render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass("".concat(_ctx.name))
  }, [renderSlot(_ctx.$slots, "default")], 2);
}

script.render = render;

export { script as default };
//# sourceMappingURL=grid.js.map
